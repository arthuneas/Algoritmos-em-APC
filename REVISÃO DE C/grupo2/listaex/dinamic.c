#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(){

    int tamanho = 0;

    char* nome = (char*) (malloc(sizeof(nome) * sizeof(char)));

    if (nome == NULL){
        return NULL;
    }

    


    return 0;
}